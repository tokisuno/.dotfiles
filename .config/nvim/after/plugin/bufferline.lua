require("bufferline").setup{
    options = {
        themable = true,
        numbers = "ordinal",
        indicator = "underline",
        separator_style = "thin",
        diagnostics = "nvim_lsp"
    }
}
